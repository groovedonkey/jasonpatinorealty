/* ============================================
   Site Configuration
   Update API_BASE_URL when the Render backend is ready.
   ============================================ */

const CONFIG = Object.freeze({
  API_BASE_URL: 'https://jasonpatinorealty.onrender.com/api',
  CONTACT_ENDPOINT: '/contact',
});
